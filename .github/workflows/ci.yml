name: CI

on:
  push:
    branches:
      - main
      - master
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

env:
  PYTHON: python3
  BUILD_JOBS: "2"

jobs:
  build-and-test:
    name: Build and test
    runs-on: ubuntu-24.04
    container:
      image: gcc:16
    timeout-minutes: 120

    steps:
      - name: Install system packages
        run: |
          apt-get update
          apt-get install -y --no-install-recommends \
            ca-certificates \
            curl \
            unzip \
            git \
            file \
            binutils \
            python3 \
            python3-dev \
            python3-venv \
            pkg-config
          rm -rf /var/lib/apt/lists/*

      - name: Check out repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Cache xmake packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.xmake/packages
            ~/.xmake/cache
          key: ${{ runner.os }}-gcc16-xmake-${{ hashFiles('xmake.lua', 'packages/l/libtorch-bin/xmake.lua') }}
          restore-keys: |
            ${{ runner.os }}-gcc16-xmake-

      - name: Install xmake
        run: |
          bash scripts/bootstrap_xmake.sh
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          export PATH="$HOME/.local/bin:$PATH"
          xmake --version

      - name: Configure environment
        run: |
          echo "PYTHON=$PYTHON" >> "$GITHUB_ENV"
          python_include="$($PYTHON -c 'import sysconfig; print(sysconfig.get_path("include"))')"
          echo "PYTHON_INCLUDE_DIR=$python_include" >> "$GITHUB_ENV"
          source scripts/env.sh
          bash scripts/check_env.sh

      - name: Configure debug build
        run: |
          source scripts/env.sh
          xmake f -m debug --yes --python="$PYTHON"

      - name: Build tensor arithmetic test
        run: |
          source scripts/env.sh
          xmake build -j "$BUILD_JOBS" typetorch_tensor_arithmetic_test

      - name: Run tensor arithmetic test
        run: |
          source scripts/env.sh
          xmake run typetorch_tensor_arithmetic_test

      - name: Build Python C API extension
        run: |
          source scripts/env.sh
          xmake build -j "$BUILD_JOBS" typetorch_capi_ext

      - name: Report debug symbol snapshot
        run: |
          source scripts/env.sh
          MAX_LINES=80 bash scripts/nm_size_probe.sh debug

      - name: Configure release build
        run: |
          source scripts/env.sh
          xmake f -m release --yes --python="$PYTHON"

      - name: Build release probes and benchmark
        run: |
          source scripts/env.sh
          xmake build -j "$BUILD_JOBS" binary_size_libtorch_probe
          xmake build -j "$BUILD_JOBS" binary_size_tensor_probe
          xmake build -j "$BUILD_JOBS" typetorch_forwarding_benchmark

      - name: Run release probes
        run: |
          source scripts/env.sh
          xmake run binary_size_libtorch_probe
          xmake run binary_size_tensor_probe

      - name: Report release binary size snapshot
        run: |
          source scripts/env.sh
          MAX_LINES=80 bash scripts/nm_size_probe.sh release
